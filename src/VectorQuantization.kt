class VectorQuantization {
    fun LBGAlgorithm(image: ImageMatrix, colorsNumber: Byte): ImageMatrix {
        val representatives = mutableListOf<Color>()
        val quantizationRegions = mutableListOf<Set<Pixel>>()
        return image
    }
}